version: "3.7"

services:

  haproxy:
    image: "${NODE_HAPROXY_IMAGE}"
    networks:
      a0_proxynet:
      bitcoin:
      mempool:
    ports:
      - "${NODE_BITCOIND_RPC_IP}:${NODE_BITCOIND_RPC_PORT}:8332"
    sysctls:
      net.ipv4.ip_unprivileged_port_start: 0
